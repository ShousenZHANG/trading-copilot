---
description: Current free-data investment guidance directly in conversation, with deterministic evidence checks and local decision memory.
argument-hint: <TICKER | GOLD.CNY> [accumulation | tactical]
---

Follow `investment-chat` for `$ARGUMENTS`. Natural-language requests use the same
workflow; no Markdown report is required. Call the shared trading-copilot MCP:
context, collect_market_snapshot, assess_investment_proposal. The returned policy
decision is authoritative. Record no actual fill unless the user says one happened.

Use a single investment-advisor when delegation helps. Supply its snapshot_id and
portfolio context. Its proposal still passes through the same deterministic gate.
Do not append the advisor's rating to the legacy five-tier memory file; the shared
tool stores its structured decision in the SQLite journal.
